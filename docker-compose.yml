version: '3.8'

services:
  risk-analyzer:
    build: .
    volumes:
      - ./documents:/app/documents
      - ./reference_documents:/app/reference_documents
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    command: --document documents/sample_contract.txt  # Default command, can be overridden 